const DRAW_POINT_RADIUS = 3;
const SELECT_RADIUS = 20;
const BOARDMAP_SECTOR_SIZE = 20;    // equals SELECT_RADIUS



// useful functions
function $(s) {
    return document.querySelector(s);
}
function $$(s) {
    return document.queruSelectorAll(s);
}